_satellite.pushAsyncScript(function(event, target, $variables){
  // Pixels: Head*
// Loads the main script tag
twttr.conversion.trackPid('nuymb', { tw_sale_amount: 0, tw_order_quantity: 0, tw_product_id: _satellite.getVar('sku') });
});
