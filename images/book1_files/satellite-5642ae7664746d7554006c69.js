_satellite.pushAsyncScript(function(event, target, $variables){
  if (window.r_validateDirectCallRule) {
    window.r_validateDirectCallRule('Product Photo View', 'ATG-4608', ['digitalData.product', 'digitalData.event'], 'photoView', []);
}
});
